Les événements de la semaine :
<% for (var i=0; i < events.length; i++) { %>
- <%= events[i].sommaire %> de <%= events[i].startDate %> à <%= events[i].endDate %>
<% if (events[i].location) { %> (ici)[<%= events[i].location %>]<% } %> 

<% } %>