      </section>
    </main>
    <footer>
      <% if (currentUser) { %>
        <p id="bottom">
          <a href="/users">Accueil</a> / Utilisateur : <a href="/users/<%= currentUser.id %>"><%= currentUser.id %></a> / <a href="/emails">Tous les emails</a> / <a href="/logout">Déconnexion</a>
        </p>
      <% } %>
    </footer>
  </body>
</html>
